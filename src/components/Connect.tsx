import React, { Component } from "react";
//import { socket } from "../App";

class Connect extends Component {
  componentDidMount() {
    // socket.emit("message", "Holy Crap");
    // socket.emit("register", {email: "matthew@matthew.com", password: "test", displayName: "Matthew"}, (response: any) =>{
    //   console.log(response);
    // })
  }
  render() {
    return <div>test</div>;
  }
}

export default Connect;
